{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "toolhive-webhook.namespace" . }}
  labels:
    {{- include "toolhive-webhook.labels" . | nindent 4 }}
{{- end }}
